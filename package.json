{
  "name": "logifera-array-architects-mvp",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free, embeddable Array Architects MVP; current Logifera repository unavailable",
  "scripts": {
    "check": "node --check intervention-core.js && node --check adaptive-engine.js && node --check array-architects.js && node --check telemetry.js",
    "test": "node --test tests/*.test.cjs",
    "start": "node server.cjs"
  },
  "engines": { "node": ">=18" }
}
